name: flutter_application_sqlite
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^2.12.0

dependencies:
  flutter:
    sdk: flutter

  sqflite: 
  path:
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:    
    - assets/db/